"use client"
import { styled } from '@mui/system';
import AppBar from '@mui/material/AppBar';

export const StyledHeaderBar = styled(AppBar)(({ theme }) => ({
  top: 0,
}));

export const StyledSubHeaderBar = styled(AppBar)(({ theme }) => ({
  top: 56,
}));

